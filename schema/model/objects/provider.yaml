id: https://open-and-sustainable.github.io/revaise-model/schema/objects/provider
name: revaise_provider
description: "Provider hosting an AI model."
imports:
  - linkml:types
  - ./slots
classes:
  Provider:
    description: "Provider or service hosting an AI model."
    slots:
      - provider_id
      - name
      - url
      - terms_of_use
    slot_usage:
      provider_id: { identifier: true }
