id: https://open-and-sustainable.github.io/revaise-model/schema/objects/prompt
name: revaise_prompt
description: "Prompt definition."
imports:
  - linkml:types
  - ./slots
  - ./enums
classes:
  Prompt:
    description: "Reusable prompt object."
    slots:
      - prompt_id
      - role
      - template
      - variables
      - rendered
    slot_usage:
      prompt_id: { identifier: true }
      role: { range: PromptRole }
